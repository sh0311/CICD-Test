spring:
  application:
    name: hamsung_project
  datasource:
    url: jdbc:mysql://localhost:3306/hamsung?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: nsh0311
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect